<UserControl
    x:Class="DevOps.UI.PullRequestPage"
    x:ClassModifier="internal"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:DevOps.UI.ViewModels"
    mc:Ignorable="d" 
    d:DesignHeight="450" d:DesignWidth="800"
    Loaded="OnLoaded"
    Unloaded="OnUnloaded"
    Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}"
    DataContext="{Binding ViewModel, RelativeSource={RelativeSource Mode=Self}}">

    <UserControl.Resources>
        <DataTemplate DataType="{x:Type vm:PullRequestVM}">
            <StackPanel Orientation="Horizontal" Margin="2">
                <TextBlock><Run Text="{Binding Author, Mode=OneWay}"/>, <Run Text="{Binding Title, Mode=OneWay}" /></TextBlock>
            </StackPanel>
        </DataTemplate>
    </UserControl.Resources>

    <Grid>
        <ListBox ItemsSource="{Binding PullRequests}" BorderThickness="0" />
    </Grid>
</UserControl>
