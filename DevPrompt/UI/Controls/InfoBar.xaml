<UserControl x:Class="DevPrompt.UI.Controls.InfoBar"
             x:ClassModifier="internal"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="28" d:DesignWidth="300"
             DataContext="{Binding RelativeSource={RelativeSource Mode=Self}}">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisible" />
    </UserControl.Resources>
    <Grid>
        <Button
            Grid.Row="1"
            BorderThickness="1"
            BorderBrush="#FF0000"
            Background="#FFCCCC"
            Padding="5"
            Click="OnClickBar"
            HorizontalContentAlignment="Left"
            Visibility="{Binding HasText, Converter={StaticResource BoolToVisible}}">
            <TextBlock Text="{Binding Text}" TextWrapping="NoWrap" TextTrimming="CharacterEllipsis" ToolTip="{Binding Text}" />
        </Button>
    </Grid>
</UserControl>
